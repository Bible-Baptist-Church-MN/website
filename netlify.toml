[build]
  command = "npm run build"
  publish = "dist/public"

# Serve Sanity Studio as SPA (redirect all /studio/* routes to /studio/index.html)
[[redirects]]
  from = "/studio/*"
  to = "/studio/index.html"
  status = 200
  force = true

[build.environment]
  NODE_VERSION = "20"
